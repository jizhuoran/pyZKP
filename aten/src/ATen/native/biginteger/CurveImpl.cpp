#include <iostream>
#include <ATen/native/biginteger/CurveImpl.h>

#pragma clang diagnostic ignored "-Wmissing-prototypes"

int aaa() {

    // const fr_t inverse_roots_of_unity[3] = {
    //     fr_t((vec256){ 1, 0, 0, 0}),
    //     fr_t((vec256){  1, 0, 0, 0 }),
    //     fr_t((vec256){  2, 0, 0, 0 }),
    // };

    // auto a = inverse_roots_of_unity[0] * inverse_roots_of_unity[1];
    // // std::cout << a << std::endl;
    // // std::cout << inverse_roots_of_unity[2] << std::endl;
    // std::cout << inverse_roots_of_unity[1] << std::endl;
    return 0;
}
